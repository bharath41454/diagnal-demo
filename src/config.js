const host = "https://bharath41454.github.io/diagnal/";
const pageData = `${host}pageData-<index>.json`;
const rowLimit = 3; // Number of cards to be rendered in a row.

export { host, pageData, rowLimit };
